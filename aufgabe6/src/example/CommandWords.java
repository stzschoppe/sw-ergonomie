 

/** * Class CommandWords - Teil des Abenteuerspiels. * * Diese Klasse verwaltet die Liste aller im Spiel bekannten Befehlsnamen und * wird zur Analyse von Benutzereingaben herangezogen. */class CommandWords{    // ein konstantes Array mit allen gueltigen Befehlsnamen    private static final String validCommands[] = {        "farbig", "schwarz", "Hilfe", "stop"    };    /**     * Constructor - ohne Aufgabe     */    public CommandWords()    {        // nichts zu tun ...    }    /**     * Prüfen, ob eine gegebene Zeichenreihe ein gueltiges Befehlswort ist.      **/    public boolean isCommand(String aString)    {        for(int i = 0; i < validCommands.length; i++)        {            if(validCommands[i].equals(aString))                return true;        }        // Zeichenreihe nicht bei den Befehlsnamen gefunden, also:        return false;    }    /*     * Alle gueltigen Kommandos ausdrucken.     */    public void showAll()     {        for(int i = 0; i < validCommands.length; i++)        {            System.out.print(validCommands[i] + "  ");        }        System.out.println();    }}