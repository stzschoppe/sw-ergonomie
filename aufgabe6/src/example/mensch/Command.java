package mensch;  

/** * Class Command. * * Derzeit werden Kommandos unterstuetzt, die neben dem Befehlswort  * optional einen Parameter enthalten. *  * Zunaechst wird geprueft, ob das Befehlswort bekannt ist; falls nein, * wird null zurueckgegeben. * * Wenn der Befehl nur aus dem Befehlswort besteht, ist der Parameter null. * * Im Moment wird der Parameter nicht weiter geprueft. Kuenftig koennte * man auch mehr als einen Parameter unterstuetzen */class Command{    private String commandWord;    private String secondWord;    /**     * Kommando-Objekt aus Befehlswort und Parameter zusammensetzen.     * Beiden koennen null sein.     */    public Command(String firstWord, String secondWord)    {        commandWord = firstWord;        this.secondWord = secondWord;    }    /**     * "getter" fuer Befehlswort.     */    public String getCommandWord()    {        return commandWord;    }    /**     * "getter" fuer Parameter.     */    public String getSecondWord()    {        return secondWord;    }    /**     * Handelt es sich um ein gueltiges Befehlswort?     */    public boolean isUnknown()    {        return (commandWord == null);    }    /**     * Hat der Befehl einen Parameter?     */    public boolean hasSecondWord()    {        return (secondWord != null);    }}